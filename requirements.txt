# Core CRANE merge pipeline + baselines.
# Tested with Python 3.11 on Linux + 4× H100 80GB.
# For benchmark eval (vLLM serving, OpenHands, swebench, terminal-bench),
# see requirements-eval.txt.

torch>=2.0.0
transformers>=4.36.0
huggingface_hub>=0.19.0
safetensors>=0.4.0
accelerate>=0.24.0
datasets>=2.14.0
numpy>=1.24.0
pyyaml>=6.0
tqdm
